<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
   <mapper namespace="com.situ.xuexiao.dao.CategoryDao">
   		<resultMap type="Category" id="findCategory">
   			<id column="sequenceNO" property="sequenceNO"/>
	  		<result column="ID" property="ID"/>	
	  		<result column="parentID" property="parentID"/>	
	  		<result column="title" property="title"/>	
   		</resultMap>
   		
   		<select id="findCourseType" parameterType="String" resultMap="findCategory">
   			select * from category where parentID = 7 and ID != #{ID};
   		</select>
   		
   		<select id="findAddCourseType" resultMap="findCategory">
   			select * from category where parentID = 7
   		</select>
   
   		<select id="findAskLeaveType" resultMap="findCategory">
   			select * from category where parentID = 13
   		</select>
   </mapper>